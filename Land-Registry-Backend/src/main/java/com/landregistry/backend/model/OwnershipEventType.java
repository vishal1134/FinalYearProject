package com.landregistry.backend.model;

public enum OwnershipEventType {
    LAND_REGISTERED,
    LAND_VERIFIED,
    LAND_REJECTED,
    TRANSFER_REQUESTED,
    TRANSFER_APPROVED,
    TRANSFER_REJECTED
}
